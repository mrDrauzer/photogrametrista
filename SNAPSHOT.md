# Состояние проекта (Snapshot)

## Текущий статус (Январь 2026)
Система полностью отлажена, исправлены критические ошибки фронтенда и настроена стабильная инфраструктура.

### Что работает:
1. **Frontend (React)**: 
   - Исправлены все `ReferenceError` и ошибки импорта.
   - Настроена корректная маршрутизация Nginx для SPA (теперь страницы доступны по прямым ссылкам).
   - Реализовано центрирование карты по снимкам и скрыта атрибуция Leaflet.
2. **Backend (Django)**: 
   - База данных полностью мигрирована.
   - Настроена идемпотентная инициализация демо-данных.
   - Расширен импорт с Яндекс.Диска (поддержка публичных ссылок).
3. **Инфраструктура**:
   - Устранены конфликты портов (8001, 3001, 3101, 8080).
   - Все контейнеры успешно взаимодействуют в сети `photogrametrista_net`.

## Следующие шаги
- Интеграция реальных алгоритмов фотограмметрии (OpenDroneMap или др.) в Celery задачи.
- Настройка экспорта в S3 для долгосрочного хранения артефактов.

## Команды для управления
```powershell
# Запуск
docker-compose up -d --build

# Инициализация (обязательно при первом запуске)
docker-compose exec django-app python manage.py migrate
docker-compose exec django-app python create_demo_admin.py
```

---
Подробная история изменений в файле `SESSION_HISTORY.md`.
Дата обновления: 2026-01-13
